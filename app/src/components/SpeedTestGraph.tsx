import {
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  CardDescription,
} from "@ui/card";

const SpeedTestGraph = () => {
  return (
    <Card className="w-1/2 h-full">
      <CardHeader>
        <CardTitle>SpeedTest Graph</CardTitle>
        <CardDescription>En cours de construction</CardDescription>
      </CardHeader>
      <CardContent>
        {/* <p className="italic">🚧 En cours de construction</p> */}
      </CardContent>
    </Card>
  );
};

export default SpeedTestGraph;
