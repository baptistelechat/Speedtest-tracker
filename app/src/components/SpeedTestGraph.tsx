import { Card, CardHeader, CardTitle, CardContent } from "@ui/card";

const SpeedTestGraph = () => {
  return (
    <Card className="w-1/2 h-full">
      <CardHeader>
        <CardTitle>SpeedTest Graph</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Card Content</p>
      </CardContent>
    </Card>
  );
};

export default SpeedTestGraph;
